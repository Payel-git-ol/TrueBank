apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: postgres-auth
  name: postgres-auth
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
