apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: C:\Users\pasaz\AppData\Local\Microsoft\WinGet\Packages\Kubernetes.kompose_Microsoft.Winget.Source_8wekyb3d8bbwe\kompose.exe convert -f docker-compose.yaml
    kompose.version: 1.37.0 (fb0539e64)
  labels:
    io.kompose.service: postgres-auth
  name: postgres-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: postgres-auth
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: C:\Users\pasaz\AppData\Local\Microsoft\WinGet\Packages\Kubernetes.kompose_Microsoft.Winget.Source_8wekyb3d8bbwe\kompose.exe convert -f docker-compose.yaml
        kompose.version: 1.37.0 (fb0539e64)
      labels:
        io.kompose.service: postgres-auth
    spec:
      containers:
        - env:
            - name: POSTGRES_DB
              value: true_db_auth
            - name: POSTGRES_PASSWORD
              value: true123
            - name: POSTGRES_USER
              value: true-server
          envFrom:
            - secretRef:
                name: postgres-secrets
          image: postgres:15
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - /usr/bin/pg_isready
                - -U
                - true-server
                - -d
                - true_db_auth
            failureThreshold: 5
            periodSeconds: 10
            timeoutSeconds: 5
            initialDelaySeconds: 30
          readinessProbe:
            exec:
              command:
                - /usr/bin/pg_isready
                - -U
                - true-server
                - -d
                - true_db_auth
            failureThreshold: 5
            periodSeconds: 10
            timeoutSeconds: 5
            initialDelaySeconds: 30
          name: postgres-server-auth
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgres-auth
      restartPolicy: Always
      volumes:
        - name: postgres-auth
          persistentVolumeClaim:
            claimName: postgres-auth
